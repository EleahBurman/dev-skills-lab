<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Skills</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <h1>Skills as of <%= time %></h1>
  <ul>
    <% skills.forEach(skill => { %>
      <li>
        <a href="/skills/<%= skill._id %>/edit">
          <button>Edit</button></a>
        <form 
          action="/skills/<%= skill._id %>?_method=DELETE"
          class="delete-form"
          method="POST">
          <button type="submit">X</button>
        </form>
        <a href="/skills/<%= skill._id %>"><%= skill.skill %></a>
        <a href="/skills/<%= skill._id %>"><%= skill.ability %></a>
        <a href="/skills/<%= skill._id %>"><%= skill.difficulty %></a>
				- 
				<%= skill.ability ? 'true' : 'false' %>
			</li>
    <% }) %>
  </ul>
  <a href="/skills/new">Add Skill</a>
</body>
</html>